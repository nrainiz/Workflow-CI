name: crop-recommendation

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_estimators: {type: int, default: 100}
      dataset: {type: str, default: Croprecommendation_preprocessing.csv}
    command: "python modelling.py {n_estimators} {dataset}"
